import { Suspense } from "react"

import { Wrapper } from "@/components/wrapper"

export default function PaymentPage() {
  return (
    <Suspense>
      <Wrapper>
        <div>Payment</div>
      </Wrapper>
    </Suspense>
  )
}
